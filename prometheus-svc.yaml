apiVersion: v1
kind: Service
metadata:
  labels:
    app: prometheus-service
  name: prometheus-service
spec:
  ports:
  - port: 9090
    protocol: TCP
    targetPort: 9090
    nodePort: 32765
  selector:
    app: prometheus
  type: NodePort
status:
  loadBalancer: {}
